{
  "name": "omalo-graphics-website",
  "private": true,
  "version": "1.0.0",
  "description": "Omalo Graphics Centre Ltd website — static site deployed on Cloudflare Pages.",
  "scripts": {
    "minify": "cleancss -o css/styles.min.css css/styles.css && terser js/script.js -o js/script.min.js --compress --mangle"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "terser": "^5.31.0"
  }
}
